@if($requestx->puededit)
<a class="btn btn-sm btn-primary" href="{{ route('valoraci.editar', $queryxxx->id) }}">EDITAR</a>
@endif
<a class="btn btn-sm btn-primary" href="{{ route('valoraci.editar', $queryxxx->id) }}">VER</a>
